module.exports = {
    name: 'emptyQueue',
    async execute(queue, track) {
        // Emitted when the player adds multiple songs to its queue
        // queue.connection.disconnect();
        // const leaveTimeout = 60000; // 60,000 milliseconds = 1 minute
        // setTimeout(() => {
        //     queue.connection.disconnect();
        //     queue.metadata.channel.send('Left the voice channel after 1 minute of inactivity.');
        // }, leaveTimeout);
        // console.log(leaveTimeout);
    }
}